import '../styles/index.css';
import HeroImage from './HeroImage';

const HeroSection = () => (
  <div className="hero-section">
    <div className="hero-title-wrapper">
      <div className="hero-title">

      </div>
    </div>
    <HeroImage />
  </div>
);

export default HeroSection;

        // <h1>Steph & Vic</h1>
        // <h1>are still doing it...</h1>
